"""S7 training wrappers around the vendored DiffSinger fork (third_party/).

Contract: docs/PIPELINE_AND_CONTRACTS.md §S7. Implemented in Migration P7.
Own vocoder training is a first-class milestone (Q4) and starts as soon as clean
stems exist — it needs no alignments (Gaelic audio included from day one, Q13).
"""
